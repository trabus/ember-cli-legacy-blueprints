import { fooBar } from '../../../helpers/foo-bar';
import { module, test } from 'qunit';

module('Unit | Helper | foo bar');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = fooBar(42);
  assert.ok(result);
});
