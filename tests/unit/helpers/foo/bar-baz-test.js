import { fooBarBaz } from '../../../../helpers/foo/bar-baz';
import { module, test } from 'qunit';

module('Unit | Helper | foo/bar baz');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = fooBarBaz(42);
  assert.ok(result);
});
